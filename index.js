const generateName = require("./nameGenerator");
const generateEmail = require("./emailGenerator");
const generateNumber = require("./numberGenerator");
const generateDate = require("./dateGenerator");

module.exports = {
  generateName,
  generateEmail,
  generateNumber,
  generateDate,
};
